{
  "name": "mi-sitio-web",
  "version": "1.0.0",
  "description": "Sitio web estático desplegado con Jenkins a S3",
  "scripts": {
    "start": "npx serve -l 3000 .",
    "test": "jest --verbose --coverage"
  },
  "devDependencies": {
    "jest": "^30.5.0"
  }
}
